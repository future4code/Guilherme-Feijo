```function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  let precoMaca
    if(quantidade <= 11 ){
    precoMaca = 1.30
  }else{
    precoMaca = 1
  }
  let custoTotal = precoMaca * quantidade
  return custoTotal
```

}```

